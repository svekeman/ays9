doc:
  property:
  - os: ''
  - threshold: Size in mega before the autoscale should add new disk
  - mount: ''
  - incrementSize: Incremental disk size
  - autoScaleLimit: AutoScaling limit. 
links:
  parent:
    auto: false
    optional: false
    role: os
recurring:
- action: autoscale
  log: true
  period: 1m
